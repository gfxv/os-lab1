version: '3.8'

services:
  uwuntu:
    image: ubuntu:latest
    container_name: os-lab1-cocker
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
    tty: true
